package com.example.Supermarket.controller.v1;

public class PaymentControllerTest {
    // Should get all payments if user has role ADMIN

    // Should get payment by its id if user has role ADMIN

    // Should not get all payments if has role CASHIER

    // Should not get payments by id if user has role CASHIER

    // Should throw exception if payments' id doesn't exits

    // Should Save Payment if user has role CASHIER

    // Should throw exception when tring to create payment with invalid fields

    // Should not create payment if there isn't enough products on stock
}
